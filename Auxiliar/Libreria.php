<?php
    require_once 'Conexion.php';
    require_once 'Modelo/Tablero.php';

    /*
        int tablero[] = new Tablero ;
        int minas = 6;
        int tableroHumano[] = new int[20];
        int posicion;
        boolean result;
        int casillas = tablero.length;
        
        generarTablero(tablero, minas);
        construirTablero(tablero);
        construirTableroHumano (tableroHumano);
        do{
             do{
                 System.out.println("Qué posición eliges?");
                 posicion = pedirPosicion();
             }while (posicion > tablero.length);
             
             result = comprobarTablero(tablero, posicion);
             
             if (!result){           
                  generarJugada(tablero, tableroHumano, posicion);
                  mostrarTableroJugador(tableroHumano);
                  casillas--;
             }
        }while ( casillas > 0 && !result );
        
        if (result){
            System.out.println("HAS PERDIDO");
        }else{
            System.out.println("LO LOGRASTES");
        }
        mostrarTablero(tablero);
     }  
 }
 */
 